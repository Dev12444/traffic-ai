# ── Slim dependency set for DEPLOYING JUST THE API (Render free tier) ────────
# The inference server never runs YOLO / OpenCV / yt-dlp, so we omit the heavy
# detection stack to keep the build small and fast. Install torch CPU wheels.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.1
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
python-dotenv==1.0.1
supabase==2.5.0
