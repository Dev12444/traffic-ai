# ── Traffic Prediction Pipeline — Person A (AI/ML) ──────────────────────────
# Pinned to stable, Render-compatible versions (Python 3.10+).

# Computer vision / detection
ultralytics==8.3.0                   # YOLOv8 detection
opencv-python-headless==4.10.0.84    # headless = no GUI deps, works on Render

# Deep learning
torch==2.3.1                         # CNN-GRU model + YOLO backend
torchvision==0.18.1

# Data
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1                  # scaling, metrics, train/test split
joblib==1.4.2                        # persist the fitted scaler

# API service
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2

# Utilities
python-dotenv==1.0.1                 # env config (Supabase keys, etc.)
requests==2.32.3                     # video download
tqdm==4.66.4                         # progress bars
yt-dlp==2024.7.16                    # YouTube traffic-video acquisition
supabase==2.5.0                      # Person B DB integration (real + mockable)

# Testing (dev only)
pytest==8.2.2
httpx==0.27.0                        # FastAPI TestClient transport
